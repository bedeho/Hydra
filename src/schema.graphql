# ====
# THIS NEEDS TO BE AUTOGENERATED FROM datamodel.prisma & uses
# './generated/prisma-client'
# ====

type Query {
  my_feed: [Post!]!
  drafts: [Post!]!
  post(id: ID!): Post
}

type Post {
  id: ID!
  published: Boolean!
  title: String!
  content: String!
}